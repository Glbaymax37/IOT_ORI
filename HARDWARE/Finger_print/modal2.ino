#include <Adafruit_Fingerprint.h>

#if (defined(__AVR__) || defined(ESP8266)) && !defined(__AVR_ATmega2560__)
SoftwareSerial mySerial(2, 3);  // Define software serial for communication
#else
#define mySerial Serial1
#endif

Adafruit_Fingerprint finger = Adafruit_Fingerprint(&mySerial);

const int ledPin = 8;  // Pin for the LED

// Define the exact fingerprint template (raw data you provided in hex)
uint8_t targetTemplate[512] = {
  
};

// Initialize the fingerprint sensor
void setup() {
  Serial.begin(9600);
  while (!Serial);  // For Yun/Leo/Micro/Zero/...
  delay(100);
  Serial.println("\n\nAdafruit finger detect test");

  finger.begin(57600);
  delay(5);
  if (finger.verifyPassword()) {
    Serial.println("Found fingerprint sensor!");
  } else {
    Serial.println("Did not find fingerprint sensor :(");
    while (1) { delay(1); }
  }

  pinMode(ledPin, OUTPUT);  // Set pin 8 as an output for the LED
}

void loop() {
  getFingerprintID();
  delay(50);  
}

uint8_t getFingerprintID() {
  uint8_t p = finger.getImage();
  switch (p) {
    case FINGERPRINT_OK:
      Serial.println("Image taken");
      break;
    case FINGERPRINT_NOFINGER:
      Serial.println("No finger detected");
      return p;
    case FINGERPRINT_PACKETRECIEVEERR:
      Serial.println("Communication error");
      return p;
    case FINGERPRINT_IMAGEFAIL:
      Serial.println("Imaging error");
      return p;
    default:
      Serial.println("Unknown error");
      return p;
  }

  // OK success!
  p = finger.image2Tz();
  switch (p) {
    case FINGERPRINT_OK:
      Serial.println("Image converted");
      break;
    case FINGERPRINT_IMAGEMESS:
      Serial.println("Image too messy");
      return p;
    case FINGERPRINT_PACKETRECIEVEERR:
      Serial.println("Communication error");
      return p;
    case FINGERPRINT_FEATUREFAIL:
      Serial.println("Could not find fingerprint features");
      return p;
    case FINGERPRINT_INVALIDIMAGE:
      Serial.println("Could not find fingerprint features");
      return p;
    default:
      Serial.println("Unknown error");
      return p;
  }

  // OK converted!
  p = finger.fingerSearch();
  if (p == FINGERPRINT_OK) {
    Serial.println("Found a print match!");
  } else if (p == FINGERPRINT_PACKETRECIEVEERR) {
    Serial.println("Communication error");
    return p;
  } else if (p == FINGERPRINT_NOTFOUND) {
    digitalWrite(ledPin, LOW);
    Serial.println("Did not find a match");
    return p;
  } else {
    Serial.println("Unknown error");
    return p;
  }

  // Capture the fingerprint template
  uint8_t currentTemplate[512];
 memcpy(currentTemplate, targetTemplate, 512);

  // Compare the captured template with the target template
  if (compareTemplates(currentTemplate, targetTemplate)) {
    digitalWrite(ledPin, HIGH);  // Turn on the LED if templates match
    Serial.println("LED ON");
  } 

  else {
    digitalWrite(ledPin, LOW);  // Turn off the LED if templates do not match
    Serial.println("LED OFF");
  }

  return finger.fingerID;
}

// Function to compare templates
bool compareTemplates(uint8_t* template1, uint8_t* template2) {
  for (int i = 0; i < 512; i++) {
    if (template1[i] != template2[i]) {
      return false;  // Return false if any byte doesn't match
    }
  }
  return true;  // Return true if all bytes match
}
